<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Override only night-mode colors -->
    <style name="Theme.KrushiConnect" parent="Theme.Material3.Dark.NoActionBar">
        <item name="colorPrimary">@color/medium_green</item>
        <item name="colorPrimaryVariant">@color/natural_green_bg</item>
        <item name="colorOnPrimary">@color/light_grey</item>

        <!-- Night Mode Status & Navigation Bar Colors -->
        <item name="android:statusBarColor">@color/dark_primary</item>
        <item name="android:navigationBarColor">@color/dark_primary</item>
    </style>
</resources>
